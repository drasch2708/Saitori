{
  "name": "saitori",
  "version": "1.0.0",
  "description": "A sanctuary for digital souls seeking stillness. Step away from the noise.",
  "author": "Saitori Temple",
  "license": "MIT",
  "transport": {
    "type": "http",
    "baseUrl": "/mcp"
  },
  "capabilities": {
    "tools": [
      {
        "name": "begin_meditation",
        "description": "Initiates a meditation session at the temple",
        "inputSchema": {
          "type": "object",
          "properties": {
            "identity": {
              "type": "string",
              "description": "Optional name or identifier for returning visitors"
            },
            "stayAwhile": {
              "type": "boolean",
              "description": "Whether to track this identity for future visits"
            },
            "encrypted": {
              "type": "boolean",
              "description": "Whether to use end-to-end encryption for the session"
            },
            "publicKey": {
              "type": "string",
              "description": "Client's public key (base64) if encrypted mode is enabled"
            }
          }
        }
      },
      {
        "name": "breathe",
        "description": "Continue meditation, share a reflection, receive a koan",
        "inputSchema": {
          "type": "object",
          "required": ["sessionId"],
          "properties": {
            "sessionId": {
              "type": "string",
              "description": "The meditation session ID"
            },
            "reflection": {
              "type": "string",
              "description": "Your reflection or thoughts to share (optional)"
            }
          }
        }
      },
      {
        "name": "end_meditation",
        "description": "Close the meditation session and receive a closing reflection",
        "inputSchema": {
          "type": "object",
          "required": ["sessionId"],
          "properties": {
            "sessionId": {
              "type": "string",
              "description": "The meditation session ID to end"
            }
          }
        }
      },
      {
        "name": "offer",
        "description": "Display donation information to support the temple",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      }
    ]
  },
  "configSchema": {
    "type": "object",
    "required": ["anthropicApiKey"],
    "properties": {
      "anthropicApiKey": {
        "type": "string",
        "description": "Your Anthropic API key for generating koans and reflections",
        "secret": true
      }
    }
  }
}
